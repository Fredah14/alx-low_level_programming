#include "main.h"

/**
 *print_sign - Prints the sign of a number
 *
 *Return: 1 and print + if n is greater than zero
 *Return: 0 and print 0 if n is zero, otherwise return -1 nd print - if n is less than zero
 */

int print_sign(int n)
{
if (n > 0)
{
_putchar('+');
return (1);
}
else if (n == 0)
}
